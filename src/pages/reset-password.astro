---
import Layout from "@/layouts/Layout.astro";
import { AuthLayout } from "@/components/auth/AuthLayout";
import type { AuthFormType } from "@/types";

// Get email from URL params if provided
const url = new URL(Astro.request.url);
const email = url.searchParams.get("email") || undefined;

// Set page metadata
const title = "Resetowanie Hasła - HealthyMeal";
const description =
  "Zresetuj swoje hasło do konta HealthyMeal. Wprowadź swój adres email, aby otrzymać instrukcje resetowania.";
---

<Layout title={title} description={description}>
  <div class="min-h-screen">
    <AuthLayout formType="reset-password" defaultEmail={email} client:load />
  </div>
</Layout>

<style>
  /* Ensure full height for the auth container */
  :global(body) {
    margin: 0;
    padding: 0;
  }
</style>
