---
import Layout from "@/layouts/Layout.astro";
import { RecipeGeneratorView } from "@/components/recipes/RecipeGeneratorView";

// Check if user is authenticated
// This will be handled by middleware, but we can add additional checks here if needed
---

<Layout title="Generuj Przepis - HealthyMeal">
  <main class="min-h-screen bg-background">
    <RecipeGeneratorView client:load />
  </main>
</Layout>
